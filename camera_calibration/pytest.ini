[pytest]
junit_family=xunit2
filterwarnings =
    # flake8 parallelizes file checks via multiprocessing(fork); Python 3.12 warns
    # when forking a multi-threaded process. Benign here, so silence the noise.
    ignore:This process .* is multi-threaded:DeprecationWarning
